import { TabPlaceholder } from './_shared';

export function InsightsTab() {
  return (
    <TabPlaceholder
      title="Insights"
      body="Performance trends across SLA, response times, and backlog health."
    />
  );
}
